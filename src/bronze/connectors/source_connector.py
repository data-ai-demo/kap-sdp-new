"""Step 1 — Connect.

Authenticate and establish access to the source system (SharePoint API,
RiskMatch download, Data Submission App) or future source.

Each connector returns a path or credential context that downstream
Auto Loader jobs use to reach the landing volume.
"""
